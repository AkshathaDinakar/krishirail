import { DisableTrainModalComponent } from './disable-train-modal.component'

describe('DisableTrainModalComponent', () => {
  it('should mount', () => {
    cy.mount(DisableTrainModalComponent)
  })
})